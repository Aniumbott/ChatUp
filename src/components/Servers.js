import React from "react";
import styled from "styled-components";

function Servers() {
  return <ServersStyleComponent></ServersStyleComponent>;
}

// Styled Componets
const ServersStyleComponent = styled.div`
  height: 100vh;
  width: 100vw;
  padding-left: 4rem;
  color: black;
`;
export default Servers;
